::

fn
  name: "confidential"
  $in:
    randomBytes:
      $t: k "function"
      $o: true
      $d: """
        Provide a custom implementation for #{k 'randomBytes'}.
        **Warning:** A robust source of entropy is necessary for
        encryption. Use this feature with caution.
      """
  $out:
    api:
      $t: k "object"
      $d: "An instance of the Confidential API."

::
